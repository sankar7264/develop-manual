{
  "name": "presidio-web",
  "version": "0.0.1",
  "description": "React web application",
  "author": "devops@t-3.com",
  "license": "UNLICENSED",
  "engines" : {
    "npm" : ">=7.0.0",
    "node" : ">=16.13.2"
  },
  "config": {
    "nodeVersion": "16.13.2",
    "yarnVersion": "1.22.19"
  },
  "scripts": {
    "install:from-lock": "yarn install --production --frozen-lockfile --cache-folder .yarn-cache",
    "install:dev": "yarn install --production=false --no-lockfile --cache-folder .yarn-cache",
    "install:dev-from-lock": "yarn install --production=false --frozen-lockfile --cache-folder .yarn-cache",
    "lint": "echo LINT TBD",
    "test": "echo TEST TBD",
    "typescript:global": "npm install -g typescript@$npm_package_dependencies_typescript"
  },
  "dependencies": {
    "@types/node": "^18.13.0",
    "typescript": "^4.8.2"
  },
  "devDependencies": {
  }
}
